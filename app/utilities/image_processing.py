import os
from PIL import Image


def process_image(image):
    # with Image.open(image) as im:
        # format = im.format
    format = ""
    size = 0
    # image.seek(0, os.SEEK_END)
    # size = image.tell()
    return size, format
    
